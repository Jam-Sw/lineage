{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Dashboard and onboarding windows: app IPC + events only. All network/git runs in Rust.",
  "windows": ["dashboard", "onboarding"],
  "permissions": ["core:default"]
}
