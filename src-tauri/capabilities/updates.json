{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "updates",
  "description": "Dashboard window only: check, download, and install app updates, then relaunch.",
  "windows": ["dashboard"],
  "permissions": ["updater:default", "process:allow-restart"]
}
